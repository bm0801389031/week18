//function statement named isSquare with parameter n
function isSquare(n) {
    //return static method Math.sqrt with parameter n modulo 1 equal zero
    
                return Math.sqrt(n) % 1 === 0;
    }
    
    